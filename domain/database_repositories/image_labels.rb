# frozen_string_literal: true

module TranslateThis
  module Repository
    # Repository for Label Entities
    class ImageLabels
      # TODO: How do we search for this relationshop?
      # We don't have ImageLabels ORM to call ImageLabelsORM.first - do we need this?
      # def self.find_id(id)
      # end
    end
  end
end
